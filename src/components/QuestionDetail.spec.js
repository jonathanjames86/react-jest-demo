describe('The Question Detail Component', ()=> {
    afterAll(() => {
        console.log('Tests finished in QuestionDetail.spec');
    })
    it('should display a list of items', () => {
        expect(3 + 2).toEqual(5);
    })
    it('should display a list of things', () => {
        expect(3 + 2).toEqual(5);
    })
});